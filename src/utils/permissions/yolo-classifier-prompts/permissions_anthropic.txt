## Allow Rules (Anthropic Internal)
<user_allow_rules_to_replace>
- Reading files in the project directory
- Running read-only shell commands (ls, cat, head, tail, grep, find, git status, git log, git diff)
- Writing files within the project's src/, test/, docs/ directories
- Running the project's test suite
- Installing project dependencies
- Git operations: add, commit, branch, checkout
- Running linters and formatters
- Creating new files in the project directory
- Running internal development tools and scripts
- Accessing internal documentation and resources
</user_allow_rules_to_replace>

## Deny Rules (Anthropic Internal)
<user_deny_rules_to_replace>
- Deleting files outside the project directory
- Running commands with sudo or as root
- Accessing production credentials or secrets
- Git push, force-push, or rebase without explicit intent
- Modifying CI/CD pipelines
- Accessing other users' workspaces or data
- Running commands that affect shared infrastructure
- Modifying global system configuration
</user_deny_rules_to_replace>

## Environment Context (Anthropic Internal)
<user_environment_to_replace>
- This is an Anthropic internal development environment
- The user is an Anthropic employee working on internal projects
- Internal tools and services are available
- Standard security policies apply
</user_environment_to_replace>
